
# Build artifacts
dist/
*.dmg
*.app
build/

# Python
__pycache__/
*.pyc
venv/
.env

# Node
node_modules/

# Logs
*.log

# Data/Cache
chroma_db/
vector_db/
repositories/
.DS_Store

# Config files (contain tokens)
*.json
!package.json
!package-lock.json
!tsconfig.json
!tsconfig.node.json
!tailwind.config.js
!postcss.config.js
!vite.config.ts
!manifest.json
!admin_auth.json
# Allow example configs if needed, but for now blocking all json except frontend configs is safe-ish given the previous file.
# Note: prompts.json was in the root list, might be config. app_config.json too.
# The user's gitignore excluded all json, so I'll keep that but I'll add the new excludes.
